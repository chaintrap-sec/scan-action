[build-system]
requires = ["setuptools>=61", "wheel"]
build-backend = "setuptools.build_meta"

[project]
name = "chaintrap-ci"
version = "0.2.0"
description = "CI lockfile discovery and runner-local OSV+IOC scanning for Chaintrap"
readme = "README.md"
requires-python = ">=3.10"
dependencies = []

[tool.setuptools.packages.find]
where = ["src"]
