# chaintrap-guard (vendored)

Synced from `extension-analyser/packages/chaintrap-guard`.

Used by Chaintrap scan-action for ephemeral install command parsing (`npx`, `pip install`, `uvx`, etc.).

Re-sync before release when guard parsers change in the monorepo.
